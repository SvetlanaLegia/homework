let product = 'Печенье «Юбилейное», 1 кг';
let inBasket = true;
let quantity = 5;

if (inBasket === true) {
    // Выводим результат, подставляя значения переменных
    console.log(quantity + ' x ' + product);
    // Или с использованием шаблонных строк (современный вариант):
    // console.log(`${quantity} x ${product}`);
}
